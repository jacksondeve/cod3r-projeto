const mongoose = require('mongoose')
moongoose.connect('mongodb://localhost/knowledge_stage', {usernewurlparser:true})
.catch(e => {
    const msg = 'ERRO! nao foi possivel cnectar com o mongoDB'
    
})